import { } from '../actions';

const defaultState = { scores: ''};

const AddScore = function (state = defaultState, action) {
  switch (action.type) {
    default: return state;
  };
  
};

export default AddScore;